README
시스템 프로그래밍
프로젝트 #3

20181668 이예진

1. 컴파일 방법
make 명령어를 입력한다.
아래의 파일이 모두 같은 폴더에 있어야 한다.
20181668.c
20181668.h
Makefile
opcode.txt
테스트 obj 파일들

2. 실행 방법
컴파일을 완료한 다음 ./20181668.out 을 입력하면 프로그램이 실행된다.
프로그램이 실행되면 sicsim> 이 화면에 출력된다.
원하는 명령어를 입력하면 사용이 가능하다.
h 또는 help를 입력하여 사용이 가능한 명령어들을 확인할 수 있다.
q 또는 quit를 입력하면 프로그램을 종료할 수 있다.
프로젝트 3에서 추가된 명령어는 progaddr, loader, run, bp이다.

명령어는 최대 100글자까지 입력이 가능하며 소문자만 인식한다.
인자는 16진수이고 대소문자 모두 가능하다.
opcode 명령어 입력시 mnemonic은 대문자만 가능하다.
assemble 명령어는 asm 파일만 가능하다.
progaddr은 0으로 초기화되어 있다.
loader 명령어는 최대 3개 파일까지만 가능하다.
bp는 주소뿐만 아니라 clear와 같이 사용할 수 있다.